#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 00; i <= 99; i++)
{
	putchar(i + '0');
	putchar(i%10 + 'o');
	if (i != 99)
{
	putchar(',');
	putchar(' ');
}
}
return (0);




}
