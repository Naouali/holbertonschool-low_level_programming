#include <stdio.h>

/**
 * main - function to generate largest palindromes
 *
 * Return: int
 */

int main(void)
{
	int i, j, m, rem;
	int value = 0;

	for (i = 100; i <= 999; i++)
	{
		for (j = 100; j <= 999; j++)
		{
			m = i*j;
		}
	}
	while(m)
	{	
		rem = m % 10;
		m = m / 10;
		
