#include "holberton.h"
#include <stdio.h>

/**
 * main - function that print a new name without recompiling in the shell
 *
 * Reaturn : is a zero
 */


int main(int argc, char* argv[])

{
	if (argc > 1)
	printf("%s", argv[0]);


	return (0);
}
