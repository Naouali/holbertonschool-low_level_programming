#include "holberton"
#include <stdio>

/**
 * _isdigit - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _isdigit(int c)

{

	if (c > 0)&&(c < 10)
	return (1);

	return (0);
}
